{% set version = "3.0.3" %}

package:
  name: pyscaffold
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/p/pyscaffold/PyScaffold-{{ version }}.tar.gz
  sha256: 702734b83d67ce2e1cdf95af827aaf890d08a5ab2cb8b113d569cbc328d485d9

build:
  noarch: python
  number: 0
  skip: True  # [py2k]
  script: python -m pip install --no-deps --ignore-installed .
  entry_points:
    - putup = pyscaffold.cli:run


requirements:
  build:
    - python >=3
    - pip
  run:
    - python >=3
    - setuptools

test:
  imports:
    - pyscaffold
    - pyscaffold.templates
  commands:
    - putup --help

about:
  home: http://pyscaffold.org/
  license: MIT
  license_file: LICENSE.txt
  summary: 'Template tool for putting up the scaffold of a Python project.'

extra:
  recipe-maintainers:
    - ocefpaf
